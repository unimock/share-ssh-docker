#!/bin/bash

if [ "$USER" = "root" ] ; then
  echo "Execution in root context not allowed!"
  exit 1
fi

passwd.orig $USER

HASH=$( sudo -i grep "^${USER}:" /etc/shadow | cut -d: -f2 )
echo "$HASH" > /home/${USER}/.sshd/shadow
rm -f /home/${USER}/.sshd/initial_ssh_password.txt
